package:
  name: nuitka
  version: !!str 0.5.9

source:
  fn: Nuitka-0.5.9.tar.gz
  url: https://pypi.python.org/packages/source/N/Nuitka/Nuitka-0.5.9.tar.gz
  md5: 55667047e4b0575047a9d4c554ab9df7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nuitka = nuitka:main
    #
    # Would create an entry point called nuitka that calls nuitka.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nuitka
    - nuitka.build
    - nuitka.codegen
    - nuitka.codegen.templates
    - nuitka.finalizations
    - nuitka.freezer
    - nuitka.gui
    - nuitka.nodes
    - nuitka.optimizations
    - nuitka.tree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nuitka.net
  license: Apache License, Version 2.0
  summary: 'Python compiler with full language support and CPython compatibility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
